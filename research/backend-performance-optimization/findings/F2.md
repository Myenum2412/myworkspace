# F2: Database Query Optimization

## Findings

### [1] B-tree indexes achieve logarithmic lookup scalability — a tree depth of 4-5 handles millions of records
- quote: "Real world indexes with millions of records have a tree depth of four or five. A tree depth of six is hardly ever seen."
- url: https://use-the-index-luke.com/sql/anatomy/the-tree
- source_type: primary
- published: unknown (continuously maintained since 2010, content current 2026)
- confidence: high

### [2] Index-only scans eliminate table access entirely when the index covers all queried columns, dramatically reducing I/O
- quote: "The index-only scan is one of the most powerful tuning methods of all. It not only avoids accessing the table to evaluate the where clause, but avoids accessing the table completely if the database can find the selected columns in the index itself."
- url: https://use-the-index-luke.com/sql/clustering/index-only-scan-covering-index
- source_type: primary
- published: unknown (continuously maintained)
- confidence: high

### [3] PostgreSQL EXPLAIN ANALYZE reveals actual row counts vs. planner estimates, enabling identification of misleading statistics
- quote: "The thing that's usually most important to look for is whether the estimated row counts are reasonably close to reality. In this example the estimates were all dead-on, but that's quite unusual in practice."
- url: https://www.postgresql.org/docs/current/using-explain.html
- source_type: primary
- published: 2026 (PostgreSQL 18 documentation)
- confidence: high

### [4] PostgreSQL supports incremental sort — an EXPLAIN-observable optimization that sorts prefix-sorted data, reducing memory and enabling early LIMIT return
- quote: "Compared to regular sorts, sorting incrementally allows returning tuples before the entire result set has been sorted, which particularly enables optimizations with LIMIT queries. It may also reduce memory usage and the likelihood of spilling sorts to disk."
- url: https://www.postgresql.org/docs/current/using-explain.html
- source_type: primary
- published: 2026 (PostgreSQL 18 documentation)
- confidence: high

### [5] ORMs produce N+1 selects by default (Hibernate, Doctrine, DBIx::Class), issuing N+1 round-trips where a single JOIN suffices — network latency dominates response time
- quote: "The ORMs don't generate SQL joins—instead they query the SALES table with nested selects. This effect is known as the 'N+1 selects problem' because it executes N+1 selects in total if the driving query returns N rows."
- url: https://use-the-index-luke.com/sql/join/nested-loops-join-n1-problem/
- source_type: primary
- published: unknown (continuously maintained, tested with Hibernate 3.6.0, JPA, Doctrine 2.0.5, DBIx::Class 0.08192)
- confidence: high

### [6] Database JOINs are always faster than ORM nested selects because they eliminate N network round-trips — latency, not bandwidth, is the bottleneck
- quote: "An SQL join is still more efficient than the nested selects approach—even though it performs the same index lookups—because it avoids a lot of network communication... the number of database round trips is more important for the response time than the amount of data transferred."
- url: https://use-the-index-luke.com/sql/join/nested-loops-join-n1-problem/
- source_type: primary
- published: unknown
- confidence: high

### [7] PostgreSQL's max_connections defaults to 100, but each connection consumes a backend process and shared memory — connection pooling is essential for scaling beyond this
- quote: "Determines the maximum number of concurrent connections to the database server. The default is typically 100 connections... PostgreSQL sizes certain resources based directly on the value of max_connections. Increasing its value leads to higher allocation of those resources, including shared memory."
- url: https://www.postgresql.org/docs/current/runtime-config-connection.html
- source_type: primary
- published: 2026 (PostgreSQL 18 documentation)
- confidence: high

### [8] PgBouncer's transaction pooling mode is the practical default for high-throughput applications, freeing server connections between transactions at the cost of breaking session-level features
- quote: "Transaction pooling: A server connection is assigned to a client only during a transaction. When PgBouncer notices that the transaction is over, the server will be put back into the pool."
- url: https://www.pgbouncer.org/features.html
- source_type: primary
- published: 2026 (PgBouncer 1.25.2)
- confidence: high

### [9] PostgreSQL implements a process-per-connection model (not thread-per-connection), meaning each connection requires a separate OS process with independent memory overhead
- quote: "PostgreSQL implements a 'process per user' client/server model. In this model, every client process connects to exactly one backend process."
- url: https://www.postgresql.org/docs/current/connect-estab.html
- source_type: primary
- published: 2026 (PostgreSQL 18 documentation)
- confidence: high

### [10] Top-N queries must use explicit LIMIT/FETCH FIRST syntax for the planner to recognize and optimize partial results — aborting client-side is equivalent to sorting the entire table
- quote: "The database can only optimize a query for a partial result if it knows this from the beginning... The response time using a pipelined execution, however, only grows with the number of selected rows."
- url: https://use-the-index-luke.com/sql/partial-results/top-n-queries
- source_type: primary
- published: unknown (continuously maintained)
- confidence: high

### [11] PostgreSQL EXPLAIN supports machine-readable output (XML, JSON, YAML) and BUFFERS option, enabling automated query plan analysis at scale
- quote: "EXPLAIN has a BUFFERS option which provides additional detail about I/O operations performed during the planning and execution of the given query. The buffer numbers displayed show the count of the non-distinct buffers hit, read, dirtied, and written."
- url: https://www.postgresql.org/docs/current/using-explain.html
- source_type: primary
- published: 2026 (PostgreSQL 18 documentation)
- confidence: high

### [12] Bulk data loading performance requires creating indexes after data insertion, not during — incremental index maintenance is dramatically slower
- quote: "If you are loading a freshly created table, the fastest method is to create the table, bulk load the table's data using COPY, then create any indexes needed for the table. Creating an index on pre-existing data is quicker than updating it incrementally as each row is loaded."
- url: https://www.postgresql.org/docs/current/populate.html
- source_type: primary
- published: 2026 (PostgreSQL 18 documentation)
- confidence: high

## Dead ends
- MySQL 8.0 optimization overview (dev.mysql.com) returned 403 Forbidden; content unavailable for direct sourcing
- PgBouncer FAQ and configuration pages were not fetched as the features page provided sufficient information on pooling modes
- Database-agnostic connection pooling literature (HikariCP, SQLAlchemy pool) not directly fetched; PgBouncer docs were chosen as they represent the canonical production solution

## Suggested follow-ups
- How does HikariCP's connection pool sizing algorithm compare to PgBouncer's transaction mode for ORM-heavy Java applications?
- What are the quantified performance differences between B-tree, GIN, GiST, and BRIN index types in PostgreSQL for specific workload patterns (full-text search, time-series, JSON)?
- How do prepared statement caches in connection poolers interact with PostgreSQL's prepared statement lifecycle, and what are the latency implications?
