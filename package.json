{
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm ci --include=dev && npm run build && cp -r public ../public"
  },
  "dependencies": {
    "next": "^16.2.9",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
